// entities/RemotePlayer.js
export class RemotePlayer {
    constructor(peerId) {
        this.peerId = peerId;
    }

    update(delta) {
        // Stub: Will interpolate incoming network transform updates
    }
}
